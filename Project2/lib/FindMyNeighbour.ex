defmodule FindMyNeighbour do

  def full(id) do
	#returns the neighbouring ids to send msg to 
  end

  def line(id) do
	#returns the neighbouring ids to send msg to 
  end

  def rand2D(positions,myposition,id) do
	#SPECIAL since it takes positions of other and self
	#returns the neighbouring ids to send msg to 
  end

  def 3Dtorus(id) do
	#returns the neighbouring ids to send msg to 
  end

  def honeycomb(id) do
	#returns the neighbouring ids to send msg to 
  end

  def randhoneycomb(id) do
	#returns the neighbouring ids to send msg to 
  end

end
